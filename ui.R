library(shiny)shinyUI(pageWithSidebar(headerPanel("Mean distribution for the sum of exponential random variables",            ),sidebarPanel(h3('Input the data for the simulation'),numericInput('nrv', 'Number of exponential random variables to sum', 30, min = 2, max = 200,step=1),numericInput('param', 'Parameter of the exponential', 0.5, min = 0, max = 1,step=0.01),sliderInput("nsim","Number of simulations", min = 1,max = 10000, value = 100    , step=50),checkboxInput("flag","Show normal curve", value = FALSE),submitButton('Submit')),mainPanel(tabsetPanel(    tabPanel("Results",    h3('Result of simulation'),h4('Simulated mean'),verbatimTextOutput("mean"),h4('Theoretic mean'),verbatimTextOutput("mean_teor"),h4('Simulated variance'),verbatimTextOutput("var"),h4('Theoretic variance'),verbatimTextOutput("var_teor"),plotOutput('newHist')                    ),    tabPanel("Documentation",    p('This app is built to analyze the mean distribution for the sum of exponential random variables.'),p('The following parameters can be chosen: number of exponential of random variables that have to be used, parameter of the exponential, number of simulations (the last with a slider).'),p('Once the submit button is clicked, the app will then perform the chosen number of simulations and for each will compute mean and variance of the simulation. In the result tab this two values will be compared with the theoretical values.'),p('In the result panel also the histogram of the density of the mean is shown. If the checkbox Show normal curve is selected, the normal distribution will also be drawn, allowing the comparison with the simulation result.')    )  ))))